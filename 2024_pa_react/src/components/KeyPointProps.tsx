import * as React from "react";import 'bootstrap/dist/js/bootstrap.bundle.min';interface KeyPointProps {	imageSrc: string;	title: string;	description: string;}const KeyPointProps: React.FC<KeyPointProps> = ({   imageSrc,   title,   description,}) => {	return (		<div className="col-md-4 my-4 px-4">			<img src={imageSrc} alt={title} className="mb-3" width="80" />			<h4>{title}</h4>			<p>{description}</p>		</div>	);};export default KeyPointProps;